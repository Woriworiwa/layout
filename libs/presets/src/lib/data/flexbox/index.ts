import { flexRowsPreset } from './flex-rows.preset';
import { flexColumnPreset } from './flex-column.preset';

import { alignItemsPreset } from './align-items.preset';
import { justifyContentPreset } from './justify-content.preset';
import { centeredContentPreset } from './align-justify-centered.preset';
import { flexGrowShrinkPreset } from './flex-grow-shrink.preset';

export const flexPresets = [
  flexRowsPreset,
  flexColumnPreset,
  flexGrowShrinkPreset,
  alignItemsPreset,
  justifyContentPreset,
  centeredContentPreset
];
